[project]
name = "fe"
version = "0.1.0"
description = "Streamlit frontend app for the monorepo"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "pandas>=2.3.3",
    "streamlit>=1.55.0",
]
