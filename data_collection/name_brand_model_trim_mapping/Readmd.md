The main purpose of this directory is to 
create a mapping between raw training data to the tuple (brand ,  model , trim)
so that it can be used for training. 

the names of each element of tuple is being listed in the sources/
